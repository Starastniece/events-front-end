'use strict';

describe('myApp.eventRegister module', function() {

  beforeEach(module('myApp.eventRegister'));

  describe('event registration controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var eventRegisterCtrl = $controller('EventRegisterCtrl');
      expect(eventRegisterCtrl).toBeDefined();
    }));

  });
});
