'use strict';

describe('myApp.userRegister module', function() {

  beforeEach(module('myApp.userRegister'));

  describe('user registration controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var userRegisterCtrl = $controller('UserRegisterCtrl');
      expect(userRegisterCtrl).toBeDefined();
    }));

  });
});
